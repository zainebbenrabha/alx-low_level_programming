#include "maih"
/**
 * _puts_recursion - function like puts();
 * @s: input
 * Return: Always 0
 */
void _put_recursion.c
{
	if (*s)
	{
		_putchar(*s);
		_puts_recursion(s + 1);
	}
	
	else
		_putchar('\n')
}
